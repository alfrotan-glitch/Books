# راهنمای کامل نصب و اجرای سامانه روی دسکتاپ ویندوز
**سامانه هوشمند استخراج، بازسازی و ممیزی متن کتاب از فایل‌های PDF**  
**نسخه پروداکشن (Windows Desktop Ready)**

---

## 🚀 روش راه‌اندازی سریع با سه مرحله ساده

### مرحله ۱: دانلود (Clone) پروژه روی دسکتاپ
پنجره **PowerShell** یا **Command Prompt** را در ویندوز باز کرده و دو خط زیر را اجرا کنید:

```powershell
cd "$env:USERPROFILE\Desktop"
git clone -b arena/01a0d9ec-books https://github.com/alfrotan-glitch/Books.git BOOK-TEXT-EXTRACTOR
```

با این کار، پوشه کامل پروژه با نام `BOOK-TEXT-EXTRACTOR` روی دسکتاپ شما قرار می‌گیرد.

---

### مرحله ۲: نصب پیش‌نیازها و ساخت آیکون دسکتاپ (فقط یک‌بار)
وارد پوشه `BOOK-TEXT-EXTRACTOR` شوید و روی فایل زیر دوبار کلیک کنید:

👉 **`نصب_و_راه_اندازی.bat`** (یا **`setup.bat`**)

این اسکریپت به صورت کاملاً خودکار:
1. پایتون سیستم شما را بررسی می‌کند.
2. محیط مجازی اختصاصی ایزوله (`.venv`) می‌سازد.
3. تمامی کتابخانه‌های لازم (شامل موتور OCR، پردازش تصویر OpenCV، ماژول‌های تحلیل چندستونه و وب‌سرور) را نصب می‌کند.
4. **آیکون میانبر اختصاصی با نام «سامانه استخراج کتاب» مستقیماً روی دسکتاپ ویندوز شما ایجاد می‌کند.**

> **نکته مهم:** اگر پایتون روی ویندوز شما نصب نیست، ابتدا [Python 3.10+](https://www.python.org/downloads/) را دانلود و نصب کنید و حتماً تیک **"Add Python to PATH"** را فعال نمایید.

---

### مرحله ۳: شروع به کار با یک دابل‌کلیک
از این پس برای استفاده از نرم‌افزار، کافی است:

- روی آیکون **«سامانه استخراج کتاب»** روی صفحه دسکتاپ ویندوز خود دوبار کلیک کنید.
- یا فایل **`شروع_برنامه.bat`** (یا **`run.bat`**) را در پوشه پروژه اجرا کنید.

برنامه به صورت خودکار سرور محلی را فعال کرده و **میزکار گرافیکی (Web Dashboard)** را در مرورگر شما در نشانی زیر باز می‌کند:
👉 `http://localhost:8000`

---

## 🖥 ویژگی‌های میزکار گرافیکی (Web UI Dashboard)

1. **کشیدن و رها کردن فایل‌ها (Drag & Drop):** فایل‌های PDF کتاب را مستقیماً داخل کادر مرورگر بکشید یا دکمه «انتخاب فایل کتاب» را بزنید.
2. **پایش زنده فرآیند استخراج (Live Progress):** درصد پیشرفت صفحه به صفحه، وضعیت هر صفحه با رنگ‌بندی شفاف (سبز: متن بومی سالم، آبی: متن استخراج‌شده با OCR، زرد: نیازمند بازبینی) نمایش داده می‌شود.
3. **پیش‌نمایش آنلاین در محیط نرم‌افزار:** بدون نیاز به باز کردن نرم‌افزارهای جانبی، متن کتاب استخراج‌شده را در داخل همان محیط وب بخوانید یا در حافظه کپی کنید.
4. **دانلود مستقیم خروجی‌ها:**
   - 📥 **فایل متنی اصلی (`.txt`):** متن شسته‌رفته کتاب بدون ستون‌های درهم، با پاراگراف‌های یکپارچه و پاورقی‌های مرتب‌شده.
   - 📊 **گزارش تصویری کیفیت (`_report.html`):** نمایش امتیاز کیفی صفحات، جدول‌ها و نمودار توزیع متدها.
   - 📋 **مانیفست ممیزی عدم کسری (`_provenance.json`):** ممیزی ریاضی صفر کسری، مقایسه کاراکتر خام و نهایی، و هش‌های SHA-256.

---

## 📁 ساختار پوشه‌های کاری برای کاربران دسکتاپ

| نام پوشه | کاربرد |
|---|---|
| **`inbox/`** | پوشه ورودی کتاب‌ها؛ هر فایل PDF را که اینجا کپی کنید در لیست برنامه ظاهر می‌شود. |
| **`output/`** | پوشه خروجی‌ها؛ متن‌های استخراج‌شده (`.txt`)، گزارش‌های HTML و فایل‌های JSON ممیزی اینجا ذخیره می‌شوند. |
| **`assets/`** | آیکون‌های رسمی برنامه برای میانبر دسکتاپ (`app_icon.ico` و `app_icon.png`). |
| **`checkpoints/`** | ذخیره وضعیت لحظه‌ای صفحات جهت امکان توقف و ادامه مجدد کتاب‌های بسیار قطور بدون هدررفت زمان. |

---

## ⚙️ گزینه‌های پیشرفته برای کاربران خط فرمان (Power Users)

اگر مایلید بدون محیط گرافیکی و مستقیماً از طریق ترمینال کار کنید:

```cmd
:: استخراج سریع یک کتاب خاص
run.bat --cli inbox\MyBook.pdf

:: اجرای مجموعه آزمون‌های بنچ‌مارک ۱۲ گانه موتور استخراج
run.bat --benchmark

:: بازرسی ساختار داخلی و فورنزیک لایه‌های PDF
run.bat --inspect inbox\MyBook.pdf
```

---

## 🛠 رفع اشکالات متداول در ویندوز (Troubleshooting)

1. **پیام "Python is not installed":**  
   پایتون در ویندوز نصب نیست یا در PATH قرار نگرفته است. هنگام نصب پایتون تیک *Add python.exe to PATH* را فعال کنید و یک‌بار ویندوز یا پنجره ترمینال را ببندید و باز کنید.
2. **خطای ساخت Shortcut در PowerShell:**  
   اگر به دلیل سیاست‌های امنیتی ویندوز، آیکون روی دسکتاپ ساخته نشد، کافی است روی فایل `ایجاد_میانبر_دسکتاپ.bat` راست‌کلیک کرده و "Run as administrator" را بزنید، یا یک میانبر دستی (Right Click > Send to > Desktop) از فایل `شروع_برنامه.bat` ایجاد کنید.
3. **پورت ۸۰۰۰ اشغال است:**  
   می‌توانید برنامه را روی پورت دلخواه اجرا کنید:  
   `.venv\Scripts\python.exe -m src.cli serve --port 8080`
